<%= @countries.map do |country|
  {
    :id => country.id,
    :name => country.name,
    :latitude => country.latitude,
    :longitude => country.longitude,
    :link => country_path(country.id),
    :size_factor => country.disbursed_amount_factor,
    :overlay_text => format_for_currency(country.disbursed_amount / 1000000000, country.currency) + "bn",
    :marker_image_url => "/images/magenta-marker.png"
  }
end.to_json.html_safe %>

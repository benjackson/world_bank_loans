<%= @countries.map do |country|
  {
    :id => country.id,
    :name => country.name,
    :latitude => country.latitude,
    :longitude => country.longitude,
    :link => country_path(country.id),
    :size_factor => country.disbursement_factor,
    :overlay_text => "#{country.disbursement_percentage.round}%",
    :marker_image_url => "/images/orange-marker.png"
  }
end.to_json.html_safe %>

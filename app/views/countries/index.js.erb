<% @countries.each do |country| %>
  setTimeout(function() { 
    $.getJSON("<%=country_path(country, :format => :json)%>", function(data) {
      $.WorldBank.Country.create(data);
    });
  }, 100);
<% end %>

$.WorldBank.drawCountries = function() {
  // Load the countries onto the map
  <% @countries.each do |country| %>
    $.WorldBank.Country.create(<%=country_hash(country).to_json.html_safe%>);
  <% end %>
}

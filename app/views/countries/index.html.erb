<div>
  <% @countries.each do |country| %>
    <%= link_to country.name, country_path(country) %> 
    has <%= country.number_of_projects %> projects<br/>
  <% end %>
</div>

<div id="map">
  <%# Map goes here %>
</div>
